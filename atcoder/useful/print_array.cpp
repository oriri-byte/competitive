# include <bits/stdc++.h>
using namespace std;

template <typename T>
void print_array(const vector<T>& arr) {
    
    for (size_t i = 0; i < arr.size(); ++i) {
        cout << arr[i] << " ";
    }
    cout<< endl;
}